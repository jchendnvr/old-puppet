# This dir is used to store custom facts whose use would not be limited 
# to just one module.
#
# The factes, rather than being loaded with the normal structure of 
# modulename/lib/facter/something.rb 
# They are instead loaded to the ${rubysitedir}/facter dir

